# Généré par scripts/seal-secrets.sh (cible `theme`) — NE PAS éditer à la main.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: quizup-theme-secret
  namespace: quizup-prod
spec:
  encryptedData:
    QUIZUP_DB_PASSWORD: CHANGE_ME
    SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_SERVER_CLIENT_CLIENT_SECRET: CHANGE_ME
  template:
    metadata:
      name: quizup-theme-secret
      namespace: quizup-prod
    type: Opaque
