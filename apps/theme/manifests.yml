apiVersion: apps/v1
kind: Deployment
metadata:
  name: quizup-theme
  namespace: quizup-prod
  labels:
    app.kubernetes.io/part-of: quizup
    app.kubernetes.io/component: axon
    app: quizup-theme
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/part-of: quizup
      app: quizup-theme
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: quizup
        app.kubernetes.io/component: axon
        app: quizup-theme
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: quizup-theme
          image: ghcr.io/quizup-organization/theme:1.0.0
          imagePullPolicy: IfNotPresent
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
          envFrom:
            - configMapRef:
                name: quizup-common-config
            - configMapRef:
                name: quizup-oauth-client-config
            - configMapRef:
                name: quizup-theme-config
            - secretRef:
                name: quizup-theme-secret
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            failureThreshold: 40
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: quizup-theme
  namespace: quizup-prod
  labels:
    app.kubernetes.io/part-of: quizup
    app.kubernetes.io/component: axon
    app: quizup-theme
spec:
  selector:
    app.kubernetes.io/part-of: quizup
    app.kubernetes.io/component: axon
    app: quizup-theme
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: quizup-theme-config
  namespace: quizup-prod
data:
  QUIZUP_DB_NAME: quizup_theme
  # URL publique du serveur OpenAPI (acces via le gateway).
  MICROSERVICE_SWAGGER_SERVER_URL: https://api.quizup.cnadjim.fr/theme-service
  QUIZUP_SEED_DATA_ENABLED: "true"
