# Généré par scripts/seal-secrets.sh — NE PAS éditer à la main.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: quizup-infra-secret
  namespace: quizup-prod
spec:
  encryptedData:
    POSTGRES_PASSWORD: CHANGE_ME
  template:
    metadata:
      name: quizup-infra-secret
      namespace: quizup-prod
    type: Opaque
