# Généré par scripts/seal-secrets.sh (cible `ghcr-pull`) — NE PAS éditer à la main.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ghcr-pull
  namespace: quizup-prod
spec:
  encryptedData:
    .dockerconfigjson: CHANGE_ME
  template:
    metadata:
      name: ghcr-pull
      namespace: quizup-prod
    type: kubernetes.io/dockerconfigjson
