apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nas-cnadjim-fr-tls
  namespace: quizup-prod
spec:
  secretName: nas-cnadjim-fr-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - nas.cnadjim.fr
