apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: nas-insecure
  namespace: quizup-prod
spec:
  # Le NAS présente son propre certificat (interne / Synology) : on ne le vérifie pas.
  insecureSkipVerify: true
